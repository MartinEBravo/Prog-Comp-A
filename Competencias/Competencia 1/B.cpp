#include <bits/stdc++.h>
using namespace std;

vector <vector <int> <>>

int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);
    int t;
    cin >> t;
    for (int k = 0; k < t; k++){
        int n, m;
        cin >> n >> m;
        for (int i = 0; i < n; i++){
            for (int j = 0; j < m; j++){
                int a;
                cin >> a;

            }
        }
    }
}